class DateUtilsHelper {
  /// Get current UTC time
  // static DateTime nowUtc() {
  //   return DateTime.now().toUtc();
  // }
  //
  // /// Format UTC as ISO8601 string
  // static String nowUtcIso() {
  //   return DateTime.now().toUtc().toIso8601String();
  // }
  //
  // /// Convert any DateTime to UTC
  // static DateTime toUtc(DateTime dateTime) {
  //   return dateTime.toUtc();
  // }
  //
  // /// Parse an ISO string into DateTime (always UTC)
  // static DateTime fromIso(String isoString) {
  //   return DateTime.parse(isoString).toUtc();
  // }
}
